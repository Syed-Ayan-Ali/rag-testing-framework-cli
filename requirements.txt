# Python dependencies for rag-cli-tester PDF parser
# Install with: pip install -r requirements.txt

# PDF parsing library
pypdfium2>=4.0.0

# Optional: Additional PDF processing capabilities
# pypdfium2-render>=0.0.1  # For rendering PDFs to images
# pypdfium2-utils>=0.0.1   # For additional utilities
